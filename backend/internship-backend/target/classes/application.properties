# =============================================
# Application Configuration
# =============================================
spring.application.name=internship-backend
server.port=${PORT:8085}

# =============================================
# Server Configuration for Render Deployment
# =============================================
server.address=0.0.0.0
server.forward-headers-strategy=FRAMEWORK

# =============================================
# Database Configuration (MySQL - TiDB Cloud Database)
# =============================================
spring.datasource.url=${DB_HOST:jdbc:mysql://gateway01.eu-central-1.prod.aws.tidbcloud.com:4000/internship_db?useSSL=true&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&enabledTLSProtocols=TLSv1.2,TLSv1.3}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=${DB_USER:3jMrabZWjdqqmw9.root}
spring.datasource.password=${DB_PASSWORD:6QWm64HjDfYkgQLc}


# Connection pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.socket-timeout=60000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# =============================================
# Database Configuration (H2 - In-Memory - DISABLED)
# =============================================
# spring.datasource.url=jdbc:h2:mem:internship_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# =============================================
# JPA / Hibernate
# =============================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.sql.init.mode=never
# spring.sql.init.schema-locations=classpath:schema-final.sql
spring.jpa.defer-datasource-initialization=true

# =============================================
# JWT Configuration
# =============================================
app.jwt.secret=3d6f424a5a1e8c2b9d0f7e4a1b8c3d2e9f0a7b6c5d4e3f2a1b0c9d8e7f6a5b4
app.jwt.expiration=86400000

# CORS
# =============================================
app.cors.allowed-origins=http://localhost:3000,http://127.0.0.1:3000,https://*.onrender.com,https://*.vercel.app,https://national-internship-i7zp.onrender.com,https://national-internship-frontend.onrender.com

# =============================================
# Logging
# =============================================
logging.level.com.internship=INFO
logging.level.org.springframework.security=WARN

# =============================================
# Actuator Configuration
# =============================================
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always
management.health.probes.enabled=true

# =============================================
# Email Configuration (SMTP)
# =============================================
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=patrictuyisenge4@gmail.com
spring.mail.password=rgdw ryxa ukvp wjwa
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=60000
spring.mail.properties.mail.smtp.timeout=60000
spring.mail.properties.mail.smtp.writetimeout=60000
spring.mail.properties.mail.debug=true
spring.mail.properties.mail.smtp.ssl.trust=*
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2,TLSv1.3
